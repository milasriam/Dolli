# Loaded by docker-compose for the `api` service (safe defaults for local Docker only).
JWT_SECRET_KEY=docker-compose-dev-only-replace-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

ALLOW_LOCAL_AUTH=1
ALLOW_EMAIL_PASSWORD_AUTH=1
ALLOW_PASSWORD_RESET=0

DEBUG=false
